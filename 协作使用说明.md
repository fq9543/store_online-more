# 协作使用说明 — JSONBin 方案

> 适合 2–5 人小团队，3 秒内同步所有人改动。无需登录，只填一次昵称。

**✅ 已配置完成，BIN_ID = `69fca5e5c0954111d8ee3040`，可以直接分发使用。**

---

## 🚀 分发给团队（1 步）

把 `plan.html` 发到团队群里，让每个人：

1. 下载到本地，双击打开（或访问 GitHub Pages 链接）
2. 首次弹框填昵称（建议用真名或花名）
3. 右上角出现 🟢 LIVE 即表示连上了，可以开始一起编辑

**改动会在 3 秒内同步到所有人。**

---

## 🔧 常见问题

### Q: 有人改了，我这边没更新？
A: 3 秒一次轮询，最多 3 秒延迟。如果始终不更新，看右上角状态点：
- 🟢 LIVE = 正常
- 🟡 SAVING = 正在推送
- 🔴 OFFLINE = 网络问题或 KEY 失效

### Q: 两个人同时改同一格会怎样？
A: **最后一次保存的覆盖前者**。不过本机 1.5 秒内刚敲的字段被保护，不会被远端拉回覆盖。

### Q: 我想重置所有数据？
A: 去 https://jsonbin.io 登录，找到 bin，手动删掉内容，或直接删除这个 bin 后让 plan.html 重新创建一个（把 `HARDCODED_BIN_ID` 改回 `"__BIN_ID__"` 并清掉浏览器的 `__collab_bin_id` LocalStorage）。

### Q: 我的 JSONBin 免费额度够用吗？
A: 够。免费版 **10 000 次请求/月**。5 个人按每人每天活跃 4 小时、每 3 秒轮询一次算，一个月 ≈ 7 200 次，再加上写入 ≈ 8 000 次，刚好。

### Q: 我想改昵称？
A: 点右上角"改昵称"按钮。

### Q: 安全吗？Key 公开在 HTML 里不会被盗用吗？
A: 这个 Key 是你 JSONBin 账号的 **Master Key**，理论上能读写你所有 bin。真要谨慎的话可以：
1. 进 JSONBin → API Keys → 创建一个 **Access Key**（有限权限），替换掉 HTML 里的 Master Key
2. 只有团队内部用的话，风险可控（最坏情况别人改你的 plan 数据）

---

## 📦 关键信息

- **JSONBin API Key**（已嵌入 plan.html）：`$2a$10$vTmFRmkFCvEPj2GD8IjaJeTupc4kH41RshCtBStP1TaIi1Rj3WdOe`
- **BIN_ID**（已嵌入 plan.html）：`69fca5e5c0954111d8ee3040`

---

祝协作愉快 ✨
